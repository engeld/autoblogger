#include <stdio.h>

int main (void)
{
        char     linktxt_path[256];   /* path to the location of your link.txt */

        scanf("%s",linktxt_path);
        printf("%s\n",linktxt_path);

        return 0;
}
